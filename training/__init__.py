"""Training pipeline: datasets, supervised training, metrics.

Phase 2 provides the supervised path (:mod:`training.supervised`) and the dataset
builder (:mod:`training.dataset`). Reinforcement / curriculum modules arrive in later
phases (PLAN.md §7).
"""
